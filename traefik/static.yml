api:
    dashboard: true

log:
    level: DEBUG
    filePath: "/var/log/traefik/traefik.log"
accessLog: {}
#    filePath: "/var/log/traefik/access.log"
#    bufferingSize: 100

entryPoints:
    web-secure:
        address: ":443"

providers:
    docker:
        exposedbydefault: false
        network: traefik

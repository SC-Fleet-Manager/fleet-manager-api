Welcome to Fleet Manager!
To confirm your email address, simply copy-paste the link below on your internet browser.

{{ url('security_registration_confirmation', {'id': user.getId().toString(), 'token': user.getRegistrationConfirmationToken() }) }}

This email has been sent to {{ user.email }}.
You're getting this email because you've linked your email to Fleet Manager.
